[build]
  # 前端静态文件发布目录
  publish = "public"
  # Netlify Functions 目录
  functions = "netlify/functions"

[functions]
  # Netlify 免费套餐函数超时上限 10 秒
  timeout = 10

# 把 /api/* 重定向到函数，让前端统一用 /api/xxx 路径
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
