[build]
  # 前端静态文件发布目录
  publish = "public"
  # Netlify Functions 目录
  functions = "netlify/functions"

[functions]
  # 免费套餐最大超时 26 秒；DeepSeek 响应慢时留足时间
  timeout = 26

# 把 /api/* 重定向到函数，让前端统一用 /api/xxx 路径
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
